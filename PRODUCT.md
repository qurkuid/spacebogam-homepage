# PRODUCT.md — 공간보감 (spacebogam.kr)

> 이 파일은 제품 진실(product truth)의 정본이다. 디자인·카피·소재 작업은 여기 적힌 사실을
> 넘어서는 주장을 만들 수 없다. 사실이 바뀌면 코드보다 이 파일을 먼저 고친다.
> Last verified: 2026-08-22

## What this is

부산 지역 인테리어 시공사 **공간보감**의 마케팅 웹사이트. 정적 사이트(GitHub Pages,
custom domain `spacebogam.kr`)이며 상담 접수만 백엔드(INTM)로 넘긴다. 커머스도, 로그인도,
계정도 없다. 방문자가 하는 일은 단 하나 — **상담을 시작하는 것**이다.

## Who it is for

| 세그먼트 | 실태 | 1차 CTA |
|---|---|---|
| 주거 (아파트·주택 리모델링) | 본업. 시공 사례 1,047장 보유 | 상담 신청 폼 `/consultation/apply/` |
| 상업 — 사무실 | **실적 0건.** 광고 1차 대상 | **전화 사전 확인 → 대면상담 예약** (폼 아님) |
| 상업 — 일반상가 | **실적 0건.** 광고 A/B 대상 | **전화 사전 확인 → 대면상담 예약** (폼 아님) |
| 상업 — 병원·미용실·필라테스 | 실적 0건. 미용실·필라테스는 이미 광고 집행 후 실패 | 현재 비활성 |

주거와 상업은 **분리된 퍼널**이다. 상업 방문자에게 주거 질문(아파트 평형, 동호수, 입주일)을
묻지 않는다.

## Product truth — 넘을 수 없는 선

1. **상업 시공 사례는 0건이다.** 포트폴리오 1,047장 중 상업 0장. `projects` 테이블에는
   업종 컬럼 자체가 없다. 상업 페이지에 시공 사진·사례·"○○건 시공" 류를 넣을 수 없다.
   상업 랜딩의 시각 언어는 사진이 아니라 **색면과 타이포**다.
2. **견적을 미리 말할 수 없다.** 업종·전용면적·현재 상태·오픈 희망일·설비 조건(전기·급배수·
   환기·소방·간판)·임대인 규정을 확인하기 전에는 금액이 산출되지 않는다.
   → 금지어: `무료견적` `즉시견적` `실시간 견적` `가격 보장` `최저가` `평당 ○○만원`
   → 대신 쓰는 문장: **"정확한 견적은 전화상담과 현장 확인 후 안내드립니다."**
3. **PII 노출 금지.** 고객명, 상세 주소, 계약 금액, 아파트 동호수는 어떤 페이지에도 쓰지 않는다.
4. **전화번호는 표시·발신 이원 체계다.** 사람 눈에 보이는 모든 표기(본문·버튼 라벨·
   aria-label·JSON-LD `telephone`·문서)는 대표번호 **`1551-0163`** 하나만 쓴다.
   실제 발신·문자 타깃(`tel:`/`sms:` href, JS 동적 주입, 생성 템플릿)은 추적용
   안심번호 **`050713881252`** 정규형 하나만 쓴다. 안심번호를 하이픈 표기로 화면에
   노출하는 것, 대표번호를 `tel:`/`sms:` 타깃으로 쓰는 것은 둘 다 계약 위반이다.
   신규 번호 발급·대체 번호 표기 금지. 표시 문자열은 홈의 `officialPhoneDisplay`를 따른다.
5. 상업 랜딩의 성공 = **전화 사전 확인을 거쳐 대면상담이 예약되는 것**. 상업 퍼널은
   광고 → 랜딩 → 전화 사전 확인(업종·면적·일정) → 대면상담 예약 → 현장 확인 →
   견적·제안 순이다. 전화(`tel:`)는 대면상담으로 가는 1차 경로이지 상담의 종착지가
   아니다 — 통화로 상담이 끝난다는 인상이나 "통화 ○분" 류의 검증 안 된 약속을 카피에
   쓰지 않는다. 콜백 요청 폼은 전화 연결이 실패했을 때만 보조로 제공하며(대면상담
   일정을 잡기 위한 콜백), 폼을 1차 CTA 자리에 올리지 않는다.

## Surfaces

| 경로 | 모드 | 성공 정의 |
|---|---|---|
| `/` (홈) | Persuade | 상담 신청 진입 |
| `/consultation/apply/` | Operate | 주거 상담 폼 제출 |
| `/commercial/call/` | **Persuade** | `tel:` 클릭 → 전화 사전 확인 → 대면상담 예약 |
| 사례 상세 (`/case-*`) | Experience | 시공 결과가 먼저 보일 것 |
| `/blog/` | Read | 이해하고 상담으로 이동 |

`/commercial/call/`은 쿼리 파라미터로 업종 variant를 받는다: `office`(기본) / `shop`.
`clinic`은 코드에 존재하나 현재 광고 대상이 아니다.

## Visual truth — incumbent

라이브가 실제로 렌더하는 브랜드 정본은 **`assets/preview-v8.css`** 하나다.

```
--paper: #f6f3ed   /* 배경. 흰색 아님 */
--ink:   #1e1d1a   /* 글자·CTA 채움. 검정 아님 */
--line:  #d9d4ca   /* 헤어라인 */
```

- 서체: **SUIT Variable** (`assets/SUIT-Variable.woff2`). 시스템 스택은 폴백일 뿐이다.
- **액센트 색이 없다.** 강조는 색이 아니라 크기·여백·헤어라인으로 만든다.
- 제목은 **크고 얇다** (`font-weight: 400`). 굵은 헤드라인은 브랜드 위반이다.
- CTA는 **직각**. `border-radius`로 알약(999px) 만들지 않는다.
- 그라데이션·드롭섀도 없음. 카드 박스보다 헤어라인 구분선을 쓴다.
- 헤더는 `.v8-header` 규격 — 높이 74px, 하단 헤어라인.

**`assets/commercial.css`는 죽은 파일이다.** 레거시 4개 페이지만 참조하는 고아 팔레트
(`--brown:#b06743`, 알약 버튼, SUIT 미로드). 새 작업에서 절대 로드하지 않는다.
`assets/site.css`도 같은 이유로 옛 판이다. 2026-08-22 브랜드 FAIL의 원인이 정확히 이것이었다.

## Measurement contract

- 퍼널 이벤트명은 **DB의 CHECK enum**이다. 마이그레이션 없이 새 이름을 보내면 400이 난다.
  분해가 필요하면 이벤트명을 새로 만들지 말고 `utm_content`로 쪼갠다.
- `/commercial/call/`의 `tel:` 링크는 `data-v8-event="phone_click"`을 단다.
  마크업만으로는 발화하지 않는다 — `preview-v8.js`가 로드되어야 하고,
  그 파일의 `FUNNEL_ALLOWED` 목록에도 이벤트명이 들어 있어야 한다. 둘 다 확인한다.
- `phone_click`은 측정 가능한 **상류 보조지표**로 유지한다. 최종 판정은 CRM(INTM
  `consult_req`)에서 대면상담으로 이어진 유효 상담이다.
- UTM은 `commercial_vertical`, `creative`, `keyword`를 보존한다.
  단, 홈 A/B 버킷이 `page_variant`를 덮으므로 광고 각도 판정은 **`utm_content`로** 한다.

## Constraints

- 순수 정적 HTML/CSS/JS. 빌드 스텝도 프레임워크도 없다. 파일이 곧 배포물이다.
- 배포는 GitHub `main` 머지로만 일어난다. 다른 원격은 배포를 트리거하지 않는다.
- 상담 제출 API는 무인증 CORS이며 `spacebogam.kr` 오리진만 허용한다.
  → **프리뷰·로컬에서는 상담 폼이 절대 뜨지 않는다.** 이는 결함이 아니다.
- 대표(1인 의사결정자)는 개발자가 아니다. 승인 요청은 파일 경로·SQL이 아니라
  **눈에 보이는 변화 하나**로 제시한다.
