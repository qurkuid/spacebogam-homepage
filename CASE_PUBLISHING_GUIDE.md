# 사례(포트폴리오) 발행 가이드

작성: 개발팀장 · NEW-5 산출물 · 2026-08-12

CEO(또는 콘텐츠 담당자)가 개발 도움 없이 포트폴리오 사례를 추가·수정할 수 있도록 만든
최소 발행 흐름입니다. 기존 8개 사례 페이지(대우이안, 메가센텀 등)는 손으로 만든 페이지라
그대로 두고, 이 흐름은 **새로 추가하는 사례**를 대상으로 합니다.

## 1. 이미지 규격

| 항목 | 기준 |
|---|---|
| 형식 | JPEG 또는 WebP만 (PNG/HEIC은 발행 전 변환) |
| 파일당 용량 | 썸네일 500KB 이하, 현장 사진(전/후) 1.2MB 이하 |
| 썸네일 | 4:5 세로 비율, 가로 800px 이상 |
| 현장 사진(전/후) | 4:3 비율, 가로 1200px 이상 |
| 색공간 | sRGB |
| 파일명 | 영문 소문자·하이픈만 (예: `living-remodel-after.jpg`), 한글/공백/특수문자 금지 |
| 저장 위치 | `assets/case/<슬러그>/파일명.jpg` (예: `assets/case/haeundae-35py/thumb.jpg`) |

**alt 텍스트 형식**: `[공간 유형] [프로젝트명] [시공 전/후] 사진`
(예: "거실 리모델링 시공 후 사진"). 얼굴·브랜드 로고 등 식별 정보는 넣지 않습니다.

이 규격은 `case-admin.html`과 `scripts_build_case.py`가 자동으로 검사합니다
(파일 용량·가로 해상도·형식·파일명 규칙). 통과하지 못하면 발행되지 않고 이유가 표시됩니다.

## 2. 사례 발행 방법 (개발 도움 없이)

1. 브라우저에서 `case-admin.html`을 엽니다 (로컬 관리 비밀번호로 로그인).
2. "새 사례"를 눌러 제목·위치·평형·한 줄 요약·본문 설명(200~400자)을 채웁니다.
3. 위 이미지 규격에 맞춘 사진을 `assets/case/<슬러그>/` 폴더에 넣고, 경로와 alt 텍스트를 입력합니다.
4. **사진 사용 허가**를 반드시 `허가`로 설정합니다. 이게 아니면 절대 게시되지 않습니다.
5. 화면 하단 체크리스트가 모두 초록색(✓)인지 확인합니다.
6. 발행 상태를 `발행`으로 바꾸고 "현재 사례 저장" → "cases.json 다운로드".
7. **여기까지가 CEO/콘텐츠 담당자가 하는 전부입니다.** 다운로드된 `cases.json` 파일을
   개발팀장에게 전달해주세요 (카카오톡/메일 등, 터미널·git 작업 불필요).
8. 개발팀장이 파일을 `data/cases/cases.json`에 반영하고 `python3 scripts_build_case.py`를
   실행해 사례 페이지(`case-<슬러그>.html`)와 포트폴리오 목록(`portfolio.html`)을 생성합니다.
9. 결과를 함께 확인한 뒤 개발팀장이 실서비스에 배포합니다. (배포는 실제 고객이 보는 사이트에
   영향을 주는 작업이라 별도 확인 절차를 거칩니다.)

## 3. 발행 전 체크리스트 (스크립트가 자동 확인)

- [ ] 사진 사용 허가가 `허가` 상태인가
- [ ] 썸네일·현장 사진 모두 alt 텍스트가 채워졌는가
- [ ] 현장 사진(전/후)이 1장 이상 있는가
- [ ] 이미지 형식·용량·해상도가 규격을 충족하는가
- [ ] 슬러그가 영문 소문자·하이픈만 사용하는가
- [ ] 본문에 가격·견적 확정 표현이 없는가 (가격은 CEO 승인 없이 확정하지 않는 경계 원칙)

허가 상태가 `허가`가 아니거나 체크리스트를 통과하지 못하면 `scripts_build_case.py`가 해당
사례를 건너뛰고 이유를 출력합니다 — 실수로 허가되지 않은 사진이 게시되는 일을 막기 위한
안전장치입니다.

## 4. 현재 구현 범위 (v1)

- 새로 추가하는 사례는 히어로·사례 정보·현장 사진 갤러리·상담 CTA로 구성된 표준 템플릿을
  사용합니다. 기존 8개 사례처럼 스토리텔링 섹션(현장 이야기, Q&A)까지는 자동 생성하지
  않습니다 — 필요하면 개발팀장이 손으로 보강합니다.
- `data/cases/cases.json`에 `status: "published"`로 표시된 사례만 실제 페이지로
  만들어지고 포트폴리오 목록에 노출됩니다. `"draft"`는 만들어지지 않습니다.
- 재실행해도 안전합니다(idempotent) — 같은 사례를 다시 발행하면 기존 페이지와 목록 항목을
  덮어쓸 뿐 중복 생성되지 않습니다.
- 이 도구는 로컬/브랜치에서 결과를 만들 뿐 실서비스 배포는 하지 않습니다. 배포는 기존
  운영 절차(개발팀장 검토 후 배포)를 따릅니다.
